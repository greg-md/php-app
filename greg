#!/usr/bin/env php
<?php

/**
 * @var $app \Greg\ApplicationContract
 */
$app = require __DIR__ . '/autoload.php';

$status = $app->run(function(\Greg\Console\ConsoleKernelContract $kernel) {
    return $kernel->run();
});

exit($status);
