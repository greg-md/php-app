#!/usr/bin/env php
<?php

/**
 * @var $app \Greg\ApplicationStrategy
 */
$app = require __DIR__ . '/autoload.php';

$status = $app->run(function(\Greg\Console\ConsoleKernelStrategy $kernel) {
    return $kernel->run();
});

exit($status);
